# Виджет купонов

Встраиваемый JavaScript виджет для отображения календаря купонных выплат по облигациям.

## Особенности

- 🎨 **Два встроенных дизайна** - светлая и темная темы
- 📱 **Адаптивный дизайн** - корректно отображается на всех устройствах
- ⚡ **Легкая интеграция** - простое подключение через HTML атрибуты или JS API
- 🔄 **Динамическая загрузка данных** - поддержка загрузки через API
- 🎯 **Без зависимостей** - чистый JavaScript, не требует дополнительных библиотек
- ♿ **Доступность** - семантичная разметка и поддержка клавиатурной навигации

## Быстрый старт

### 1. Подключение

```html
<script src="index.js"></script>
```

### 2. Базовое использование

```html
<div data-coupons-widget></div>
```

### 3. С настройками

```html
<div
  data-coupons-widget
  data-theme="dark"
  data-title="Мои купоны"
  data-api-endpoint="/api/coupons"
></div>
```

## Способы использования

### Автоматическая инициализация

Добавьте атрибут `data-coupons-widget` к любому HTML элементу:

```html
<!-- Базовый виджет -->
<div data-coupons-widget></div>

<!-- С темной темой -->
<div data-coupons-widget data-theme="dark"></div>

<!-- С кастомным заголовком -->
<div data-coupons-widget data-title="Календарь выплат"></div>

<!-- Без заголовка -->
<div data-coupons-widget data-show-header="false"></div>

<!-- С загрузкой данных через API -->
<div data-coupons-widget data-api-endpoint="/api/coupons"></div>

<!-- Загрузка с MOEX по ISIN коду -->
<div
  data-coupons-widget
  data-api-endpoint="RU000A0JX0J2"
  data-title="ОФЗ 26207"
></div>
```

### Программная инициализация

```javascript
// Базовый виджет
const widget = new CouponsWidget({
  containerId: 'my-widget',
});

// С полными настройками
const widget = new CouponsWidget({
  containerId: 'my-widget',
  title: 'Календарь купонов',
  showHeader: true,
  apiEndpoint: '/api/coupons',
  data: [
    {
      paymentDate: '24.11.2025',
      rate: '24%',
      amount: '19.73₽',
      current: true,
    },
  ],
});
```

## Параметры конфигурации

| Параметр      | Тип     | По умолчанию          | Описание                                 |
| ------------- | ------- | --------------------- | ---------------------------------------- |
| `containerId` | string  | `'coupons-widget'`    | ID HTML элемента для размещения виджета  |
| `title`       | string  | `'Календарь купонов'` | Заголовок виджета                        |
| `showHeader`  | boolean | `true`                | Показывать ли заголовок                  |
| `apiEndpoint` | string  | `null`                | URL API или ISIN код для загрузки данных |
| `data`        | array   | `[]`                  | Статические данные купонов               |

## Формат данных

### Структура объекта купона

```javascript
{
    paymentDate: '24.11.2025',     // Дата выплаты (строка)
    rate: '24%',                   // Процентная ставка (строка)
    amount: '19.73₽',              // Сумма выплаты (строка)
    current: false,                // Текущий купон (boolean, опционально)
    disabled: false,               // Отключенный купон (boolean, опционально)
    description: ''                // Дополнительное описание (строка, опционально)
}
```

### Пример массива данных

```javascript
[
  {
    paymentDate: '24.09.2025',
    rate: '26.5%',
    amount: '21.78₽',
    disabled: true,
  },
  {
    paymentDate: '23.11.2025',
    rate: '24%',
    amount: '19.73₽',
    current: true,
  },
  {
    paymentDate: '23.12.2025',
    rate: '24%',
    amount: '19.73₽',
  },
];
```

## API методы

### Создание виджета

```javascript
const widget = new CouponsWidget(options);
```

### Обновление данных

```javascript
widget.updateData(newCouponsArray);
```

### Загрузка данных по ISIN или URL

```javascript
// Загрузка с MOEX по ISIN
await widget.load('RU000A0JX0J2');

// Загрузка с кастомного API
await widget.load('/api/coupons');
```

### Уничтожение виджета

```javascript
widget.destroy();
```

## API для загрузки данных

Виджет поддерживает два способа загрузки данных:

### 1. Загрузка с MOEX (Московская биржа)

Если в качестве `apiEndpoint` указан ISIN код облигации, виджет автоматически загрузит данные с официального API Московской биржи:

```javascript
// Автоматическое определение ISIN и загрузка с MOEX
const widget = new CouponsWidget({
  containerId: 'my-widget',
  apiEndpoint: 'RU000A0JX0J2', // ISIN код ОФЗ 26207
  title: 'ОФЗ 26207',
});

// Или динамическая загрузка
widget.load('RU000A0JX0J2');
```

**Формат ISIN:** 2 буквы + 10 символов (например: `RU000A0JX0J2`)

**Логика отображения купонов:**

- Показывается **только 5 ближайших купонов** (2 прошедших + текущий + 2 будущих)
- Прошедшие купоны помечаются как `disabled`
- Первый предстоящий купон помечается как `current`
- Если все купоны в прошлом, показываются последние 5

### 2. Кастомный API endpoint

Если указан обычный URL, виджет будет отправлять GET запрос и ожидать JSON ответ в формате массива купонов.

### Пример API endpoint

```javascript
// GET /api/coupons
[
  {
    paymentDate: '24.11.2025',
    rate: '24%',
    amount: '19.73₽',
    current: true,
    disabled: false,
    description: 'Основной купон',
  },
  {
    paymentDate: '24.12.2025',
    rate: '24%',
    amount: '19.73₽',
    current: false,
    disabled: false,
  },
];
```

## Стилизация

Виджет использует встроенные стили и поддерживает две темы:

- **default** - светлая тема
- **dark** - темная тема

### Кастомизация через CSS

Вы можете переопределить стили, используя CSS селекторы:

```css
/* Изменение цвета фона */
.coupons-widget {
  background-color: #custom-color;
}

/* Стилизация текущего элемента */
.coupons-widget .list-item.current {
  background-color: #custom-highlight;
  border-left-color: #custom-border;
}
```

### Основные CSS классы

- `.coupons-widget` - основной контейнер
- `.coupons-widget-header` - заголовок
- `.coupons-list` - список купонов
- `.list-item` - элемент купона
- `.list-item.current` - текущий купон
- `.list-item.disabled` - отключенный купон
- `.block` - блок информации внутри элемента
- `.tph` - текстовый элемент

## Адаптивность

Виджет автоматически адаптируется под размер экрана:

- На устройствах с шириной менее 480px блоки информации располагаются вертикально
- Используется flexbox для гибкой компоновки
- Все размеры указаны в относительных единицах

## Совместимость

- Современные браузеры (Chrome 60+, Firefox 55+, Safari 12+, Edge 79+)
- Поддержка ES6+ (классы, async/await, деструктуризация)
- Мобильные браузеры

## Лицензия

MIT License

## Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории проекта.
